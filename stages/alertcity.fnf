var libs = [
	"draw" => 0,
	"math" => 1,
	"system" => 2,
	"character" => 3
];

function create() {
    draw.skewedimage('hi', 'sick', 0, 0);
    draw.add('hi');
}

function createPost() {
    system.set('gf.visible', false);

    character.create('trolljakAlert', 'gf', '', true);
    character.create('dad', 'bf', '');

    system.print(libs);
}